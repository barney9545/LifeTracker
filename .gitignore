# ⚠️ Credentials — never commit these
.streamlit/secrets.toml
# Service-account keys etc. at the repo root (scoped to root so it doesn't
# swallow the web app's package.json / tsconfig.json).
/*.json
**/service-account*.json
**/*-key.json

# Python
__pycache__/
*.pyc
.env
venv/
.venv/

# Apps Script (lives in Google, not GitHub)
digest.gs

# Local-only secret scratch (Vercel env paste file)
VERCEL_ENV.local.txt
*.local.txt

.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
